/*
Example of tsconfig.eslint.json, here only for ESLint+TS usage.
This file indicates which files to lint, from those given by ESLint.
"files" and / or "include" are mandatory.
"exclude" is not necessary, .eslintignore is prevalent.
*/
{
	"extends": "./tsconfig.json",
	"files": [
		"./.eslintrc.js",
	],
	"include": [
		/* To lint the configuration files themselves… */
		"**/*.js",
		"**/*.ts",
	],
	"compilerOptions": {
		"baseUrl": ".",
		"types": [
			"node",
		],
	},
}
